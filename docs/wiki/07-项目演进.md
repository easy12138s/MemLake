# 项目演进

> 上一步：[开发指南](06-开发指南.md) · 返回 [首页](Home.md)

项目按"里程碑建设 → 评审修复 → 审计加固 → 功能演进"四个阶段推进，共 74 个提交。各阶段的设计与实施细节见文末规划文档索引。

## 阶段一：基础设施（M1-M3）

| 提交 | 内容 |
|------|------|
| `c1eec1a` M1 | 配置层（pydantic-settings 全量配置）与数据库基础设施（连接池/会话工厂） |
| `5993615` M2 | Embedding HTTP 客户端、审计日志、认证鉴权（Access Key + bcrypt + RBAC 常量） |
| `1c87931` M3 | 知识图谱存储层：节点 CRUD、AGE 图遍历原语（neighbors/find_path/subgraph）、节点与图事务共写 |

同时完成自定义 PostgreSQL 镜像（AGE + pgvector + zhparser 编译，`e40349b`）。

## 阶段二：核心功能（M4-M7）

| 提交 | 内容 |
|------|------|
| `e0765b5` M4 | 三引擎融合检索：向量 + 全文（zhparser）+ 图遍历，RRF 融合，统一 FilterSpec |
| `2dbd98c` M5 | 审批工作流：批次状态机、幂等 operation_id、冲突检测、审批通过事务共写 |
| `2865179` M6 | MCP 网关：FastMCP 4.0 + 四中间件（认证/RBAC/限流/审计）+ 工具分组注册 |
| `7fe2cff` M6b | 检索类与查询类工具（search_* / get_*）补齐 |
| `1017a0b` M7 | 应用 Docker 镜像与三容器端到端验证 |

随后补充 admin 引导 CLI（`memlake-bootstrap-admin`，`33dfc77`）、部署运维文档与备份恢复（`2800d78`）、Skills 文件化与自动审批雏形（`b3c763d`）。

## 阶段三：评审与审计加固

- **MVP 评审 P1 修复**（`1b09715`）：镜像源加速等部署缺陷；
- **全量代码审计**（18 项修复，`f1a00ef` → `05151e2`）：硬键冲突检测与审计项目隔离、统一嵌入文本构造与属性变更向量重算、impact_analysis 过滤 archived、工具输入模型拒绝未知字段（防嵌套错位静默丢数据）、提交前引用校验、审批 embed 入队降级与并发竞态、审计时间过滤 SQL 下推、限流桶内存治理、compose 凭据变量化、RLS 死代码清理；
- **文档一致性对齐**（`c504f55`）：阈值标定结论、协议版本策略、遍历语义与实现对齐。

这一阶段确立了多个关键取舍（见下节设计决策）。

## 阶段四：功能演进

| 主题 | 提交 | 内容 |
|------|------|------|
| 检索质量 | `a7019bf` `0143f61` `88490b3` | bge-reranker 精排 + HNSW 归一化内积；查询侧指令感知（prompt_name=query）；默认 min_score=0.5 去噪 |
| 嵌入模型升级 | `b870a09` | bge-large-zh-v1.5 → Qwen3-Embedding-0.6B（1024 维，镜像内置） |
| 审批性能 | `8acb312` `f72c874` | 审批向量化异步化（后台 worker 落库任务表 + 轮询），消除批量审批超时 |
| 节点治理 | `c0ee66d` | update_node 工具（审批流修改已入库节点） |
| Key 接入体验 | `2aca3fa` `e9990f1` | create/rotate 返回 mcp_config 与 onboarding_prompt；Key 轮换与动态 scope |
| 可观测性 | `b8acf6c` | Prometheus /metrics（两服务）+ structlog 结构化日志，request_id/actor 贯穿 |
| 宽松模式 | `c92008e` | 按 Access Key 免审批直接入库：per-Key lax_mode + 全局 LAX_MODE_ENABLED 熔断，冲突仍回审批队列 |
| system 维度 | `e056a83` | PM 需求跨项目建模：system/system_project 表、悬浮需求（project_id 可空）、两级 scope、manage_system 工具、24 工具制 |
| 验收修复 | `52ab710`→`d285fbb` | bind_keys SQL 绑定参数修复；悬浮批次审批/嵌入任务适配（D1/D2）；自动审批冲突检测按 system 收口 |
| 测试体系 | `c525da8` | 三角色 MCP 协作端到端 pytest 用例（内存客户端完全仿真，覆盖原 e2e 脚本 8 场景） |

## 关键设计决策记录

| 决策 | 取舍 |
|------|------|
| AGE 承载图而非独立图数据库 | PostgreSQL 单实例混合存储：节点表（pgvector + tsvector + JSONB）与 AGE 图事务内共写，节点与边一致性由数据库保证，免除多库同步与运维面扩大 |
| 认证每请求实时校验、不设缓存 | Key 吊销/轮换即时生效优先于吞吐（bcrypt 校验约 200-300ms CPU）；单实例吞吐上限由此约束，高吞吐需求出现时再评估带 TTL 的缓存 |
| 写操作全走审批暂存表 | 未审批内容不进检索、不进图谱；审批通过事务共写。自动审批 + 宽松模式为信任分级提供旁路 |
| 冲突检测三层架构 | 硬键（requirement_id 等）→ 关键属性 → 语义阈值 0.85；阈值配置化并附标定脚本，随嵌入模型更换重校 |
| RLS 层从代码中移除 | 应用层已做项目隔离（validate_project_access + FilterSpec），数据库层 RLS 维护成本高于收益，审计后删除死代码 |
| tools/list 按角色过滤 | 最小权限可见性：非本角色工具不出现在列表，避免 Agent 误调用与形态泄露 |
| 幂等键 (submitted_by, batch_type, operation_id) | 网络重试与客户端重放返回首次批次，落库持久化（跨重启）而非内存时间窗 |
| AGE MATCH 静默失败由应用层兜底 | Cypher MATCH 失败不报错，建边前显式校验端点节点存在，防止边静默丢失 |


